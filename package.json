{
  "scripts": {
    "dev": "yarn stop && pm2 start index.js --watch && pm2 logs",
    "stop": "pm2 stop all"
  },
  "dependencies": {
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pm2": "^6.0.14"
  },
  "devDependencies": {}
}
